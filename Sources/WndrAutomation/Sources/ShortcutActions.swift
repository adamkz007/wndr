import Foundation

public enum ShortcutActions {
    public static func createNote(title: String, body: String) {
        // Placeholder for Shortcuts integration. Implementation will call into WndrNotes module.
        print("Shortcut createNote invoked with title: \(title)")
    }
}
