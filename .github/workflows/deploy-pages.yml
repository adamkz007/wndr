name: Deploy GitHub Pages

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build static site
        run: |
          mkdir -p _site
          cp index.html _site/index.html
          if [ -f privacy.html ]; then cp privacy.html _site/privacy.html; fi
          mkdir -p _site/docs
          if [ -f docs/wndr.png ]; then cp docs/wndr.png _site/docs/wndr.png; fi
          if [ -f icon.png ]; then cp icon.png _site/icon.png; fi
          if [ -f cylinder.svg ]; then cp cylinder.svg _site/cylinder.svg; fi
          touch _site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
